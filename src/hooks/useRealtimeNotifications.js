// Real-time notification subscriptions are managed by NotificationMenu.jsx.
// This hook is kept as a no-op to avoid duplicate subscriptions.
// eslint-disable-next-line no-unused-vars
export function useRealtimeNotifications(_session) {
  // intentionally empty — NotificationMenu handles all real-time subscriptions
}
